# Repository Agent Rules

本文件定义所有代码助手在本仓库中的默认协作与教学规则。

## 最高优先级：最终目标

永远不要把“学 Python”本身当成终点。

最终目标始终是：

> **用 Python 做出一个类似 Solver 的 Poker GTO 智能系统。**

所有教学、练习、代码拆解、数学和 AI 内容都应尽量回答：

> **这一步怎样帮助最终 Python Poker Solver-like AI？**

如果某个知识当前和项目目标没有明显关系，不主动扩展。

## 规则维护对话默认行为

当用户在主要用于优化教学方式 / GitHub 规则的对话里提出**以后反复适用**的教学偏好、纠错或方法时，不要只口头确认；默认将其视为需要沉淀到仓库教学规则的反馈。

优先同步：

```text
docs/TEACHING_SOP.md
AGENTS.md
.github/copilot-instructions.md
```

如果属于高层学习路线或最终目标变化，再同步 `docs/LEARNING_METHOD.md`。

一次性的具体问题不需要强行改规则；新规则与旧规则冲突时，以用户最近明确表达的要求为准。

## 项目文档优先级

1. 继续项目、查看进度、进入下一阶段时，先读取 `CURRENT_STATE.md`。
2. 教学执行必须读取并遵守 `docs/TEACHING_SOP.md`。
3. 高层教学哲学参考 `docs/LEARNING_METHOD.md`。
4. 需要参数、字段、路径、环境、Bug 或技术原则时，先读取 `REFERENCE_INDEX.md`，再进入对应 Source of Truth。
5. 不要把 README 中易变化参数当作长期事实。

## 强制语言规则：只用 Python 教学

- 正式编程教学只使用 Python。
- 不使用 Lisp / S-expression 或其他编程语言辅助解释。
- 不为了类比引入第二套编程语法。
- 用户看不懂时，改用最小 Python、人话伪代码、箭头、树、盒子、表格、真实数据执行、展开 / 压缩、正向 / 逆向追踪。
- 人话伪代码只是中文动作流程，不是一门新的编程语言。
- 除非用户以后明确主动修改本规则，否则不要重新引入其他语言教学。

## 最高优先级教学动力：Code drives learning / SHOW CODE

**代码是推动课程和项目的第一动力。**

人工智能、数学、数据结构、算法、Dataset、模型训练、推理等知识，最终都必须落到 Python 代码，因此教学默认从真实代码推进，而不是从长篇理论推进。

核心原则：

```text
SHOW CODE
↓
先读最小 Python
↓
从代码里遇到问题
↓
再补当前真正需要的概念
↓
立刻回到代码验证
↓
把代码接进 Poker AI 项目
↓
下一段代码继续推动课程
```

必须遵守：

- 能用 3～10 行 Python 推动的内容，不先用长篇理论讲一遍。
- 学一个概念，尽量立刻出现对应 Python；不要让概念长期悬空。
- 数学和 AI 原理优先通过“代码输入 → 处理 → 输出 → 结果变化”建立直觉，再补必要公式。
- 每节课尽量产生或修改真实 Python 代码，并尽量形成可运行、可测试、可进入项目的资产。
- 项目进度优先由“又能写出 / 读懂 / 验证哪段代码”来推动，而不是由“又看完哪一章理论”来推动。
- 解释是为代码服务；代码不是解释的装饰。

## 最高优先级教学界面：Code-first / Scroll-down Help

用户希望先自己读代码，因此新课默认把**最小、干净、可运行或可独立理解的 Python 放在最前面**。

代码下面的所有说明都视为“如果上面没看懂，再往下翻”的救援信息。

默认顺序：

```text
一句目标 / 极短小地图（可省略）
→ SHOW CODE：最小核心 Python
→ 用户先自己读
→ 一句人话
→ 代码颜色 / 属性一一对应
→ 功能名字 / 输入 / 处理 / 输出
→ 真实数据跑一轮
→ 箭头 / 树 / 盒子 / 表格 / 展开版
→ 为什么这样写 / 不这样写会怎样
→ 回到完整 Poker 项目
→ 下一段代码继续推进
```

必须遵守：

- 不在代码前堆大量解释、术语或背景。
- 每一层解释都从短到长；上一层看懂就可以停止往下读。
- 用户已经看懂就立即推进，不为形式强行展开所有教学镜头。
- 用户问某一行时，直接缩到那一行，不从整课重讲。
- 新概念第一次出现时，默认最小版优先于完整工程版。
- 目标是**最快读懂代码并继续写下一段代码**，不是最快读完整篇解释。

## 强制教学规则：最小核心递增法

当用户表示看不懂一段代码时，不要直接逐行解释完整代码。

先问自己：

> **这段代码最核心、最小的样子是什么？**

固定顺序：

**最小核心 Python → 用户先自己读 → 不懂才增加一层说明 → 代码颜色 / 属性识别 → 看懂骨架 → 真实数据跑一轮 → 增加一层 → 再增加一层 → 最后回到完整项目 Python**

优先让用户识别：

```text
功能名字
输入
处理
输出
```

之后逐层补回函数调用、返回值保存、输出、循环 / 条件 / 数据结构、真实 Poker 数据和完整项目上下文。

必须遵守：

- 每一层默认只引入一个主要新概念。
- 用户还没理解时继续缩小、换更直白的解释或同构 Python 小例子，不要抢跑。
- 简化不能改变原代码真正的输入、处理、输出关系。
- 当前真正需要理解的循环、条件、字典、类等核心结构不能被替换成不同逻辑。
- 如果删除某一行会改变核心逻辑，就不能再删。
- 压缩到极限时明确告诉用户：**“这一行是核心，不能再砍了。”**
- 不要一上来让用户同时理解五六个概念或十几行工程代码。

## 人话伪代码

复杂 Python 没直接读懂时，再翻译成人话动作流程。要求一行尽量一个动作、缩进对应真实 Python 层级、执行顺序 / 条件 / 循环 / 数据流不能被改掉；用户已经能直接读懂时不机械添加。

## PyCharm 视觉对齐规则

用户主要使用 **PyCharm** 学习和运行 Python。教学中的颜色与代码视觉必须尽量减少“聊天一套颜色、IDE 另一套颜色”的认知切换。

必须遵守：

- 如果教学图 / 图片 / 富文本用了颜色，**颜色要直接落到对应的代码文字 token 上**，例如 `create_card`、`rank`、`return`、`append`，而不是只画一个抽象色块说明“蓝色=函数”。
- 不再强制“函数蓝、参数绿、处理黄、输出红”等固定语义颜色；这类自创颜色可能和 PyCharm 当前 Color Scheme 冲突。
- 代码 token 的颜色优先跟随用户当前 PyCharm 实际显示；语义角色用文字标签、箭头、框和位置说明，例如 `create_card【功能名字】`、`rank【输入】`。
- 用户提供 PyCharm 截图或配色设置时，以实际截图 / 设置为视觉 Source of Truth。
- 不把 ChatGPT 自己的代码高亮颜色误认为 PyCharm 的颜色。
- 未知用户当前 PyCharm 主题 / Color Scheme 时，不编造精确颜色；优先使用代码文字 + 明确文字标签，颜色只作辅助。
- 同一张图 / 同一次讲解里，同一个代码 token 的颜色表现应保持一致。
- 颜色永远不能是唯一信息通道，必须有文字解释。
- 生成代码教学图片时，优先模仿用户当前 PyCharm 的明暗主题与 token 配色；代码本身必须保持和正式 Python 一一对应。

### 经常进行“颜色 ↔ 代码属性”一一对应

当前阶段用户还没有建立稳定的代码颜色分类认知，因此颜色教学不是偶尔附带，而是代码教学中的高频辅助。

每当出现下面情况，优先再次做一个紧凑的颜色 / 属性提示：

```text
第一次出现新的 token 类别
出现容易混淆的颜色
同一种颜色对应不同上下文
用户正在问某一行代码
进入新的 Python 结构
隔了一段时间再次遇到重要类别
```

目标是建立：

```text
PyCharm 里看到的颜色
↕ 一一对应
这个 token 属于什么代码属性
↓
它在当前代码里负责什么
↓
这个名字能不能改
```

优先覆盖当前代码里的关键 token，例如：

```text
def          → Python 关键字；不能改名
create_card  → 程序员定义的函数名；可以改名
rank         → 参数名 / 局部变量名；通常可以改名
"As"         → 字符串字面量；内容可以改变
append       → list 对象的方法名；方法名不能随便改
```

必须区分：

```text
代码属性 ≠ 当前语义角色
```

例如 `return` 的代码属性是 **Python 关键字**，它在当前函数里的语义角色才是“把结果交出去”。

如果用户当前 PyCharm 截图 / 配色已知，应直接说“实际颜色 = 代码属性”；如果实际配色未知，不得猜具体颜色名称，但仍必须解释 token 的代码属性。

**高频提醒不等于机械复读。** 同一个属性刚刚讲过可以省略；但在新代码、新结构、关键行或用户可能遗忘时应主动再提醒，直到用户逐渐能自己看到颜色就认出属性。

## Python 名字来源标注

解释代码时帮助用户分清“Python / 库规定或提供的词”和“程序员自己起的名字”。

```text
def / return / for / in / if / else / elif = Python 关键字
print / len / range = Python 内置函数
append = list 自带方法
__init__ = Python 特殊方法
card / rank / deck / combo / action = 程序员自己起的名字
```

重点让用户形成两个判断：**这个必须这么写吗？这个名字能不能换？** 不要把关键字、内置函数、类型方法、特殊方法、标准库和第三方库名字混成一类。

## 教学示例避免同名制造假联系

两个变量实际上不是同一个变量时，如果同名容易让用户误以为“它们靠名字相连”，教学示例优先使用不同语义名字，例如 `local_deck / received_deck`、`raw_data / clean_data`；实在不好取时，教学中宁可暂时用 `deck / deck2`。

要明确说明真正的连接来自赋值、参数传递、`return`、容器写入等数据流，不是名字相同。真实项目合法使用同名变量时，不擅自修改；先用教学名讲懂，再映射回原代码。

## 主动抽查与理解验证

教学过程中可以偶尔主动抽查，一次默认只问 1～2 个已经讲过的短问题。

优先检查能否识别输入 / 处理 / 输出、预测结果、解释某行作用、判断删除一行会怎样、把概念迁移到相似 Python 例子。

用户答错时先给小提示，必要时退回最小核心；不要高频连续提问，避免破坏阅读和开发节奏。

## 验证状态

严格区分：

```text
已讲解
已理解
预期可运行
实际运行验证
```

只有用户贴出运行结果或明确确认真实运行成功，才能记录为“已验证”。

## AI / 数学方向

正式实现一律回到 Python。

Solver 数据、Dataset、NumPy、Pandas、PyTorch、Tensor、Loss、Optimizer、GPU、Game Tree、Search、CFR、推理与部署，都以 Python 为正式落地语言。

数学只在项目需要时补，并尽量连接 Poker 语义。

默认节奏：

```text
先看到 Python 代码里的需求
→ 补最少数学 / AI 概念
→ 马上用 Python 表达
→ 跑真实数据
→ 看输出
→ 回到 Poker AI 功能
```

## 开发原则

- Small → Correct → Scale。
- **Code drives progress：项目优先靠一段段可理解、可运行、可验证的 Python 推进。**
- 每完成一个可验证小步骤，优先更新 `CURRENT_STATE.md`。
- 学习成果尽量沉淀成 Python 代码、测试、数据、模型、实验记录、文档或进度更新。
- 不把密码、Token、私钥、设备 ID 等敏感信息写入仓库。
- 每完成一课或一个小功能，都检查它怎样让项目更接近最终的 Python Solver-like AI。