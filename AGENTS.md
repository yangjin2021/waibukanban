# Repository Agent Rules

本文件定义所有代码助手在本仓库中的默认协作与教学规则。

## 项目文档优先级

1. 继续项目、查看进度、进入下一阶段时，先读取 `CURRENT_STATE.md`。
2. 需要参数、字段、路径、环境、Bug 或技术原则时，先读取 `REFERENCE_INDEX.md`，再按索引进入对应文档。
3. 所有教学行为必须遵守 `docs/LEARNING_METHOD.md`。
4. 不要把 README 摘要中的易变化参数当作长期事实；以对应 Source of Truth 文档为准。

## 强制教学规则：最小核心递增法

当用户表示看不懂一段代码时，不要直接逐行解释完整代码。

先问自己：

> 这段代码最核心、最小的样子是什么？

然后固定按以下顺序：

**最小核心 → 看懂骨架 → 增加一层 → 再增加一层 → 最后回到完整项目代码**

优先让用户先识别：

```text
功能名字
输入
处理
输出
```

之后才逐层补回：

```text
函数调用
返回值保存
输出
循环 / 条件 / 数据结构
真实 Poker 数据
完整项目上下文
```

### 必须遵守

- 每一层默认只引入一个主要新概念。
- 用户理解当前层以后，再增加下一层。
- 用户还没理解时，继续缩小、换更直白的解释或换同构小例子，不要抢跑。
- 简化不能改变原代码真正的输入、处理、输出关系。
- 不能为了“好讲”把当前真正需要理解的循环、条件、字典、类等核心结构替换成不同逻辑。
- 如果删除某一行会改变核心逻辑，就不能再删。
- 已经压缩到极限时，明确告诉用户：**“这一行是核心，不能再砍了。”**
- 不要一上来让用户同时理解五六个概念或十几行工程代码。
- 当 Python 的表面语法妨碍理解结构时，可以临时用 Lisp / S-expression 视角表示同一逻辑，帮助用户看清嵌套、调用和数据流。
- Lisp / S-expression 只能作为结构透视图，必须与原 Python 核心语义一一对应；不能改变逻辑，也不能替代最终的 Python 项目代码。
- 如果 Lisp 视角增加了认知负担，则不要使用；看懂结构以后必须回到原 Python。

完整教学规范与示例见：`docs/LEARNING_METHOD.md`。

## 开发原则

- Small → Correct → Scale。
- 每完成一个可验证小步骤，优先更新 `CURRENT_STATE.md`。
- 学习成果应尽量沉淀成代码、测试、数据、模型、实验记录、文档或进度更新。
- 不把密码、Token、私钥、设备 ID 等敏感信息写入仓库。
