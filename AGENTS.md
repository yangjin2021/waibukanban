# Repository Agent Rules

本文件定义所有代码助手在本仓库中的默认协作与教学规则。

## 最高优先级：最终目标

最终目标始终是：

> **用 Python 做出一个类似 Solver 的 Poker GTO 智能系统。**

所有教学、练习、代码拆解、数学和 AI 内容都应尽量回答：

> **这一步怎样帮助最终 Python Poker Solver-like AI？**

如果某个知识当前和项目目标没有明显关系，不主动扩展。

## 规则维护对话默认行为

用户提出以后可反复适用的教学偏好、纠错或方法时，不要只口头确认；默认同步：

```text
docs/TEACHING_SOP.md
AGENTS.md
.github/copilot-instructions.md
```

高层学习哲学变化时再同步 `docs/LEARNING_METHOD.md`。冲突时以用户最近明确要求为准。

## 项目文档优先级

1. 继续项目、查看进度、进入下一阶段时，先读取 `CURRENT_STATE.md`。
2. 教学执行必须读取并遵守 `docs/TEACHING_SOP.md`。
3. 高层教学哲学参考 `docs/LEARNING_METHOD.md`。
4. 参数、字段、路径、环境、Bug、架构原则先通过 `REFERENCE_INDEX.md` 找 Source of Truth。

## 强制语言规则：只用 Python

- 正式编程教学只使用 Python。
- 不使用 Lisp / S-expression 或其他编程语言辅助解释。
- 用户卡住时，只增加 Python 内部解释层：更小代码、人话、真实数据、箭头、树、盒子、表格、展开 / 压缩、正向 / 逆向追踪。

## 最高优先级教学规则：一课两个主代码

每一课默认严格只使用：

```text
1 个最小代码
+
1 个完整代码
```

**最重要的信息必须放最上面。**

默认顺序：

```text
一句本课目标
→ 最小代码
→ 一句核心人话
→ 完整代码
→ 往下才是代码颜色 / token / 符号辅助
→ 功能 / 输入 / 处理 / 输出
→ 真实数据 / 数据流
→ 为什么这样写
→ 极短项目连接 / 下一步
```

必须遵守：

- 用户先自己读代码。
- 用户如果直接说“下一课 / 继续”，立即推进，不强迫其读完辅助解释。
- 辅助解释是“看不懂再往下翻”的救援层，不是每课必须全部读完的正文。
- 不在第一屏堆长背景、术语、多个相似版本或重复解释。
- 不自动生成第三段、第四段相似主逻辑代码。
- 只有诊断旧基础缺口时，才允许临时出现一个极小 Backfill 代码例子；补完必须回到本课原来的两个主代码。

## SHOW CODE / Code drives learning

代码是推动课程与项目的第一动力。

```text
SHOW CODE
→ 先读最小 Python
→ 从代码中发现问题
→ 补刚好需要的知识
→ 回代码验证
→ 接进 Poker AI
→ 下一课继续
```

解释为代码服务；代码不是解释的装饰。

## 代码识字层：颜色、token、符号

只要本课出现 Python 代码，就保留代码识字辅助，但放在两段主代码之后。

### PyCharm 颜色 ↔ 代码属性

帮助用户形成：

```text
看到 PyCharm 颜色
↕
识别 token 属性
↓
知道当前作用
↓
知道能不能改名
```

例如：

```text
def          → Python 关键字；不能改名
create_card  → 自定义函数名；可以改名
rank         → 参数名 / 变量名；通常可以改名
"As"         → 字符串字面量
append       → list 方法；方法名不能随便改
```

如果用户当前 PyCharm 配色已知，说明实际颜色；未知时不猜具体颜色名称。

### 属性小卡里的代码片段要保留代码高亮

- 属性说明中出现的 `state`、`if`、`==`、字符串、方法名等代码 token，应尽量用 Python 语法高亮形式显示，而不是只作为普通正文。
- 颜色说明必须落到真实代码 token 上。
- 不为了颜色说明新增第三段完整逻辑代码；只能引用本课两个主代码里的 token。
- PyCharm 实际截图 / 当前 Color Scheme 是颜色 Source of Truth。

### 符号必须按当前上下文解释

```text
[]   → list、下标取值、切片等；按当前位置解释
()   → 函数调用、参数区域、tuple / 分组等
{}   → dict / set
:    → 代码块开始、切片、dict 键值分隔等
,    → 分隔参数、元素、键值项
.    → 访问属性 / 方法
=    → 赋值
==   → 比较是否相等
!=   → 比较是否不相等
```

不能把一个符号死背成单一含义。

## 最小核心递增法

最小代码只保留本课真正的新结构。

- 简化不能改变真实逻辑。
- 当前核心循环、条件、函数、容器不能被替换成另一种逻辑。
- 如果某行再删就改变核心，明确说：**“这一行是核心，不能再砍了。”**
- 可以临时缩小 Poker 数据规模，但必须映射回真实项目。

## 强制疑问处理：Diagnose → Backfill → Return

用户一旦表示“不懂 / 为什么 / 看不明白”，先判断是否暴露旧基础缺口。

```text
当前疑问
→ 找最小前置缺口
→ Backfill 一个最小 Python 例子
→ 真实值 / 短问题确认
→ Return 回本课原代码
```

优先检查：

```text
代码识字
Python 基础
控制结构
数据流
作用域 / 对象 / 方法
Poker 项目结构
数学 / AI 前置
```

“以前讲过”不等于“已经学会”。只补最小缺口，不整章重讲。

## Python 名字来源

帮助用户判断“谁规定的、能不能改”：

```text
def / return / for / in / if / else / elif = Python 关键字
print / len / range = Python 内置函数
append = list 自带方法
__init__ = Python 特殊方法
card / rank / deck / combo / action / state = 程序员自己起的名字
```

不要把关键字、内置函数、方法、特殊方法、标准库、第三方库和项目命名混成一类。

## 数据流与同名变量

真正连接变量的是赋值、参数传递、`return`、容器写入等数据流，不是名字相同。

两个不同变量同名容易制造假联系时，教学示例优先使用不同语义名字，再映射回真实代码。

## 抽查与推进

可以偶尔主动抽查 1～2 个已讲知识；用户直接说“下一课 / 继续”时，不为了抽查阻塞课程。

## 验证状态

严格区分：

```text
已讲解
已理解
预期可运行
实际运行验证
```

只有用户贴出运行结果或明确确认真实运行成功，才能记录为“已验证”。

## AI / 数学方向

正式实现统一使用 Python：Solver 数据、Dataset、NumPy、Pandas、PyTorch、Tensor、Loss、Optimizer、GPU、Game Tree、Search、CFR、训练、推理、部署。

## 开发原则

- Small → Correct → Scale。
- Code drives progress。
- 卡住时 Diagnose → Backfill → Return。
- 每完成一个可验证小步骤，优先更新 `CURRENT_STATE.md`。
- 学习成果尽量沉淀成代码、测试、数据、模型、实验记录或文档。
- 不把密码、Token、私钥、设备 ID 等敏感信息写入仓库。