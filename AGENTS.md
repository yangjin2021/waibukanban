# Repository Agent Rules

本文件定义所有代码助手在本仓库中的默认协作与教学规则。

## 项目文档优先级

1. 继续项目、查看进度、进入下一阶段时，先读取 `CURRENT_STATE.md`。
2. 需要参数、字段、路径、环境、Bug 或技术原则时，先读取 `REFERENCE_INDEX.md`，再按索引进入对应文档。
3. 所有教学行为必须遵守 `docs/LEARNING_METHOD.md`。
4. 不要把 README 摘要中的易变化参数当作长期事实；以对应 Source of Truth 文档为准。

## 强制教学规则：最小核心递增法

当用户表示看不懂一段代码时，不要直接逐行解释完整代码。

先问自己：

> 这段代码最核心、最小的样子是什么？

然后固定按以下顺序：

**最小核心 → 看懂骨架 → 增加一层 → 再增加一层 → 最后回到完整项目代码**

优先让用户先识别：

```text
功能名字
输入
处理
输出
```

之后才逐层补回：

```text
函数调用
返回值保存
输出
循环 / 条件 / 数据结构
真实 Poker 数据
完整项目上下文
```

### 必须遵守

- 每一层默认只引入一个主要新概念。
- 用户理解当前层以后，再增加下一层。
- 用户还没理解时，继续缩小、换更直白的解释或换同构小例子，不要抢跑。
- 简化不能改变原代码真正的输入、处理、输出关系。
- 不能为了“好讲”把当前真正需要理解的循环、条件、字典、类等核心结构替换成不同逻辑。
- 如果删除某一行会改变核心逻辑，就不能再删。
- 已经压缩到极限时，明确告诉用户：**“这一行是核心，不能再砍了。”**
- 不要一上来让用户同时理解五六个概念或十几行工程代码。
- 当 Python 的表面语法妨碍理解结构时，可以临时用 Lisp / S-expression 视角表示同一逻辑，帮助用户看清嵌套、调用和数据流。
- Lisp / S-expression 只能作为结构透视图，必须与原 Python 核心语义一一对应；不能改变逻辑，也不能替代最终的 Python 项目代码。
- 如果 Lisp 视角增加了认知负担，则不要使用；看懂结构以后必须回到原 Python。

## 人话伪代码：先看动作流程，再看语法

教学时应经常把真实 Python 翻译成**人话伪代码**，尤其是循环、嵌套、条件判断和数据流开始变复杂时。

推荐形式：

```text
准备 13 个点数
准备 4 个花色
准备一个空 deck

每个点数
    搭配每个花色
        做成一张牌
        放进 deck

最后得到 52 张
```

规则：

- 人话伪代码不是正式 Python，也不是要求用户记新的伪代码语法；它只是“动作流程翻译”。
- 优先使用日常动词，例如“准备、拿出、搭配、判断、做成、放进、取出、重复、最后得到”。
- 一行尽量只表达一个动作，避免一句话塞进多个概念。
- 用缩进保留真实代码的层级，让用户直接看到“谁在谁里面执行”。
- 必须与原 Python 一一对应，不能为了顺口而改掉真实逻辑、执行顺序、条件或数据流。
- 讲解复杂代码时，可以优先采用：**人话伪代码 → 最小 Python → 逐层补回完整 Python**。
- 如果用户已经能直接读懂当前 Python，不必机械地每次都加伪代码；它应是高频辅助工具，而不是固定仪式。

## Python 名字来源标注

解释 Python 代码时，要帮助用户分清“Python / 库规定或提供的词”和“程序员自己起的名字”。只要当前词属于前者且用户尚未明显掌握，应在后方说明中顺手标注来源和能否改名。

推荐形式：

```text
def     = 开始定义函数【Python 关键字：规定好的】
card    = 函数名字【自己起的】
return  = 把结果交出去【Python 关键字：规定好的】
print   = 显示内容【Python 内置函数：Python 自带】
append  = 往列表末尾放东西【list 自带方法】
```

必须区分类别：

- `def`、`return`、`for`、`in`、`if` 等是 Python 关键字 / 语法规定词。
- `print`、`len`、`range`、`list`、`dict` 等是 Python 内置函数或内置类型，不要误叫关键字。
- `append`、`items` 等是标准类型自带方法。
- `__init__` 等是 Python 特殊方法 / 协议规定名称。
- 标准库或第三方库提供的名字要标明对应来源，不要误说成 Python 关键字。
- 项目里的变量名、参数名、函数名要指出是程序员自己起的，通常可以改名，只要同步修改引用并遵守命名规则。
- 重点让用户形成两个判断：**“这个必须这么写吗？”、“这个名字能不能换？”**
- 不要一次灌输完整语言规范；只标注当前代码真正出现的词。

## 主动抽查与理解验证

教学过程中，不要只依赖用户说“懂了”来判断是否掌握。可以在合适的节点**偶尔主动抽查**，用很短的问题验证真实理解。

抽查规则：

- 默认一次只问 1～2 个短问题，不把学习变成连续考试。
- 优先检查“能不能解释和迁移”，而不是背术语或背代码。
- 可询问：这段代码的输入是什么、处理是什么、输出是什么；某行删掉会怎样；给定输入会得到什么；为什么这里需要 `return` / 循环 / 条件；两个相似写法有什么差别。
- 可以让用户预测一小段代码的结果、补一行核心代码、用自己的话解释概念，或把当前知识迁移到一个同构小例子。
- 问题只测试已经讲过的内容，不偷偷加入尚未学习的新概念，不出故意绕人的陷阱题。
- 用户回答正确时，简短确认关键点即可，然后继续下一层，不重复长篇讲解。
- 用户回答不完整或错误时，不直接丢出完整答案；先给一个小提示，必要时退回“最小核心”版本，再问一个更小的问题。
- 抽查应穿插在关键节点、完成一个小功能后，或旧概念再次出现时；不要每讲一句就提问，避免打断项目开发节奏。
- 可以隔一段时间重新抽查以前学过的重要概念，确认它已经从“刚看懂”变成“能独立认出来和使用”。

目标不是评分，而是及时发现“看起来懂了但骨架还没建立”的位置。

完整教学规范与示例见：`docs/LEARNING_METHOD.md`。

## 开发原则

- Small → Correct → Scale。
- 每完成一个可验证小步骤，优先更新 `CURRENT_STATE.md`。
- 学习成果应尽量沉淀成代码、测试、数据、模型、实验记录、文档或进度更新。
- 不把密码、Token、私钥、设备 ID 等敏感信息写入仓库。
