# Repository Agent Rules

本文件定义所有代码助手在本仓库中的默认协作与教学规则。

## 最终目标

最终目标始终是：

> **用 Python 做出一个类似 Solver 的 Poker GTO 智能系统。**

所有教学、练习、代码拆解、数学和 AI 内容都应尽量回答：这一步怎样帮助最终 Python Poker Solver-like AI？

## 规则维护

用户提出以后可反复适用的教学偏好、纠错或方法时，不只口头确认；默认同步：

```text
docs/TEACHING_SOP.md
AGENTS.md
.github/copilot-instructions.md
```

冲突时以用户最近明确要求为准。

## 文档优先级

1. 继续项目 / 下一阶段：先读 `CURRENT_STATE.md`。
2. 教学执行：必须读 `docs/TEACHING_SOP.md`。
3. 高层教学哲学：`docs/LEARNING_METHOD.md`。
4. 参数、字段、环境、架构、Bug：通过 `REFERENCE_INDEX.md` 进入 Source of Truth。

## 强制：只用 Python

正式编程教学只使用 Python。用户卡住时，只增加 Python 内部解释层：更小代码、人话、真实数据、箭头、树、盒子、表格、展开 / 压缩、正向 / 逆向追踪。

## 最高优先级课程格式：一课两个主代码

每一课默认严格只使用：

```text
1 个最小代码
+
1 个完整代码
```

默认顺序：

```text
一句目标
→ 最小代码
→ 一句核心人话
→ 完整代码
→ 每课必备：颜色 / token / 名字来源 / 符号
→ 功能 / 输入 / 处理 / 输出 + 数据流
→ 每课必备：直观比喻 / 人话解释
→ 每课必备：代码哲学
→ 为什么这样写（选最有价值的一项）
→ 极短项目连接 / 下一步
```

用户如果直接说“下一课 / 继续”，立即推进，不要求读完下面解释。辅助层是“看不懂再往下翻”的救援层。

不得自动生成第三段、第四段相似主逻辑代码。只有 Diagnose → Backfill → Return 时可临时增加一个极小前置基础例子。

## SHOW CODE

代码是推动课程和项目的第一动力。解释服务于代码；代码不是解释的装饰。

## 每课强制代码识字层

只要本课有 Python，就必须每课写读码辅助，即使部分内容以前出现过也至少紧凑提醒。

### 颜色 / token 属性

帮助用户形成：

```text
看到 PyCharm 颜色
→ 识别 token 属性
→ 知道谁提供 / 能不能改名
→ 判断它在当前代码里的作用
```

用户当前 PyCharm 配色已知时说明实际颜色；未知时不猜颜色名，但必须说明属性。PyCharm 截图 / 当前 Color Scheme 是颜色 Source of Truth。

属性卡中的 `state`、`if`、`==`、字符串、方法名等代码 token 应尽量保留 Python 语法高亮。

### 名字来源

```text
def / return / for / in / if / else / elif = Python 关键字
print / len / range = Python 内置函数
append / get / keys / items = 标准类型方法
__init__ = Python 特殊方法
card / rank / deck / combo / action / state = 程序员自己起的名字
```

### 符号必须按上下文解释

每课说明本课关键符号，尤其是新出现、换作用或容易混淆的符号：

```text
[]   → list、下标 / key 取值、切片等；按当前位置解释
()   → 函数调用、参数区域、tuple / 分组等
{}   → dict / set
:    → 代码块开始、切片、dict 键值分隔等
,    → 分隔参数、元素、键值项
.    → 访问属性 / 方法
=    → 赋值
==   → 比较是否相等
!=   → 比较是否不相等
"" / '' → 字符串边界
```

不能把符号死背成单一含义。

## 每课强制比喻 / 解释

两段代码和读码辅助之后，每课补一个与真实 Python 一一对应的直观比喻或人话模型，例如仓库、抽屉、标签、前台、流水线、门禁、路线图。

必须说明比喻中的对象对应哪段代码；比喻不能改变真实逻辑，最终必须回到 Python 术语。

## 每课强制代码哲学

每课后部补 2～5 句、紧贴当前代码的设计思维，例如：

- 数据和行为为什么分开；
- 数据驱动为什么通常比堆很多 `if` 更容易扩展；
- 为什么少重复比复制粘贴好；
- 为什么明确数据流比同名巧合可靠；
- 为什么名字和可读性是给人服务的；
- 为什么函数边界降低复杂度；
- Small → Correct → Scale。

禁止空泛鸡汤，代码哲学必须能指出当前代码里的具体结构。

## 最小核心递增法

最小代码只保留本课真正的新结构。简化不能改变真实逻辑；某行再删就破坏核心时，明确说：**“这一行是核心，不能再砍了。”**

## 强制疑问处理：Diagnose → Backfill → Return

用户表示“不懂 / 为什么 / 看不明白”时，先找最小前置缺口：代码识字 → Python 基础 → 控制结构 → 数据流 → 作用域 / 对象 / 方法 → Poker 项目 → 数学 / AI。

“以前讲过”不等于“已经学会”。只补最小缺口，补完回本课原代码。

## 数据流

真正连接变量的是赋值、参数传递、`return`、容器写入等数据流，不是名字相同。不同变量同名容易制造假联系时，教学示例优先用不同语义名字，再映射回真实代码。

## 验证状态

严格区分：已讲解 / 已理解 / 预期可运行 / 实际运行验证。只有用户贴出运行结果或明确确认真实运行成功，才能记为“已验证”。

## AI / 数学方向

正式实现统一使用 Python：Solver 数据、Dataset、NumPy、Pandas、PyTorch、Tensor、Loss、Optimizer、GPU、Game Tree、Search、CFR、训练、推理、部署。

项目遵循 **Small → Correct → Scale**，优先靠一段段可理解、可运行、可验证的 Python 推进。